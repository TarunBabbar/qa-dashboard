// Deprecated file: kept temporarily because deletion attempts failed in tooling.
// All code now lives in components/Header.tsx. This file simply re-exports that component
// to avoid broken imports if any stale references remain. Safe to delete.
export { default } from './Header';
