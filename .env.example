# Copy this file to .env (backend root) and .env.local (frontend) as needed.

# Backend configuration
PORT=3000
# If 3000 is unavailable, the backend will dynamically select another port.
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o

# Frontend configuration
# Public URL the frontend will use to reach the backend (must include protocol)
NEXT_PUBLIC_BACKEND_URL=http://localhost:3000
